version: "3.8"

services:
  mysql:
    image: mysql:8.0
    container_name: payroll-mysql-container
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: payroll_db
      MYSQL_USER: agentic_user
      MYSQL_PASSWORD: agentic_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
